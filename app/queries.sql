CREATE TABLE TRANSACTIONS(TRANSACTION_ID INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY, SENDER VARCHAR(55), RECIPIENT VARCHAR(55), AMOUNT FLOAT(4,2), TRANSACTION_DATE datetime, FOREIGN KEY (SENDER) REFERENCES Users(Uname));
alter table users add column status varchar(20);